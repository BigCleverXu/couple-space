<layout back="{{true}}" title="纪念日">
	<t-input label="标题" align="right" value="{{formData.title}}" placeholder="请输入" data-name="title" bind:change="changeValue" />
	<!-- <x-input defaultValue="{{formData.title}}" data-name="title" bind:change="changeValue" /> -->
	<x-date-time title="纪念日" date="{{formData.date}}" dateText="{{formData.dateText}}" bind:change="change" data-name="date" />
	<view>
		<t-button theme="primary" bind:tap="submit">提交</t-button>
		<!-- <t-button theme="danger" bind:tap="delete" ghost>删除</t-button> -->
	</view>
</layout>