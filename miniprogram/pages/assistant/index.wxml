<layout back="{{true}}" title="月经助手">
	<x-date-picker hasMonth="{{false}}" bind:change="change"></x-date-picker>
	<t-calendar using-custom-navbar default-value="{{date}}" min-date="{{minDate}}" format="{{format}}" title="日期" use-popup="{{false}}" />
</layout>